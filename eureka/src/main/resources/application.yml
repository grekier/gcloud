server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
spring:
  boot:
    admin:
      client:
        url: http://${ADMIN_SERVICE_URL:localhost:8000}